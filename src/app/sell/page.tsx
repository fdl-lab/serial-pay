import { ListingForm } from "@/components/listing/ListingForm";

export default function SellPage() {
  return (
    <main>
      <nav className="nav">
        <a href="/">← トップ</a>
      </nav>
      <ListingForm />
    </main>
  );
}
